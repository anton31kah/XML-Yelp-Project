package preprocessing.models.json

case class Tip(user_id: String,
               business_id: String,
               text: String,
               date: String,
               compliment_count: Double)
